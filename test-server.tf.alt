/* ## Networking
resource "hcloud_network" "network" {
  name     = "network"
  ip_range = "10.0.0.0/16"
}

resource "hcloud_network_subnet" "network-subnet" {
  type         = "cloud"
  network_id   = hcloud_network.network.id
  network_zone = "us-east"
  ip_range     = "10.0.2.0/24"
} */

## Create a server
resource "hcloud_server" "terraform-testing" {
  name        = "terraform-testing.com"
  image       = "debian-11"
  server_type = "cx11"
  backups     = "false"
  #location    = "ash-dc1"
  datacenter  = "ash-dc1"
  #datacenter  = "fsn1-dc14"
  ssh_keys = data.hcloud_ssh_keys.all_keys.ssh_keys.*.name
  /* firewall_ids = [hcloud_firewall.terraform-testing-fw.id]
  network {
    network_id = hcloud_network.network.id
    ip         = "10.0.2.5"
    alias_ips  = [
      "10.0.2.6",
      "10.0.2.7"
    ] */
  /* public_net {
    ipv4_enabled = true
    ipv6_enabled = false
  }
  labels = {
    "tag" : "test"
  } */
  /* }
    depends_on = [
    hcloud_network_subnet.network-subnet
  ] */
}

/* ## Firewall
resource "hcloud_firewall" "terraform-testing-fw" {
  name = "terraform-testing-fw"
  rule {
    direction = "in"
    protocol  = "icmp"
    source_ips = [
      "0.0.0.0/0",
      "::/0"
    ]
  }

  rule {
    direction = "in"
    protocol  = "tcp"
    port      = "80"
    source_ips = [
      "0.0.0.0/0",
      "::/0"
    ]
  }
  rule {
    direction = "in"
    protocol  = "tcp"
    port      = "443"
    source_ips = [
      "0.0.0.0/0",
      "::/0"
    ]
  }
  rule {
    direction = "in"
    protocol  = "tcp"
    port      = "22"
    source_ips = [
      "165.22.32.53/32",
      "79.130.18.58/32"
    ]
  }
} */

## Uncomment to create a Volume 
#resource "hcloud_volume" "storage" {
#  name       = "test-volume"
#  size       = 10
#  server_id  = "${hcloud_server.test-server.id}"
#  automount  = true
#  format     = "ext4"
#}
